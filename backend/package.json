{
  "name": "exerfy-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Backend API for Exerfy workout sheet builder",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "vitest run --config vitest.config.js",
    "test:watch": "vitest --config vitest.config.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "vitest": "^2.1.8"
  }
}

